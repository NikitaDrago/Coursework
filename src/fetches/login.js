// export const fetchLogin = async (login, pass) => {
//   const users = await fetch('http://localhost:8080/api/users')
//   const findUser = users.find(item => item.login === login)
//   return findUser
// }